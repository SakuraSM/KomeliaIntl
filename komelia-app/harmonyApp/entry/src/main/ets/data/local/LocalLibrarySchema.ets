export const LOCAL_LIBRARY_SCHEMA: string[] = [
  'CREATE TABLE IF NOT EXISTS LOCAL_LIBRARY (ID TEXT PRIMARY KEY, PAYLOAD TEXT NOT NULL)',
  'CREATE TABLE IF NOT EXISTS LOCAL_BOOK (ID TEXT PRIMARY KEY, LIBRARY_ID TEXT NOT NULL REFERENCES LOCAL_LIBRARY(ID), PAYLOAD TEXT NOT NULL)',
  'CREATE TABLE IF NOT EXISTS LOCAL_PROGRESS (BOOK_ID TEXT PRIMARY KEY REFERENCES LOCAL_BOOK(ID), PAYLOAD TEXT NOT NULL)',
  'CREATE INDEX IF NOT EXISTS IDX_LOCAL_BOOK_LIBRARY ON LOCAL_BOOK (LIBRARY_ID)'
];
